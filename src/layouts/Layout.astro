---
import "../styles/global.css";
import ConsoleSignature from "../components/ConsoleSignature.astro";
import SEO from "../components/SEO.astro";
import { ClientRouter } from "astro:transitions";

interface Props {
  title?: string;
  description?: string;
  image?: string;
  canonicalURL?: URL;
  lang?: string;
}

const { title, description, image, canonicalURL, lang = "en" } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <SEO {title} {description} {image} {canonicalURL} />
    <ClientRouter />

    <!-- Cloudflare Web Analytics -->
    <script
      defer
      src="https://static.cloudflareinsights.com/beacon.min.js"
      data-cf-beacon='{"token": "f8244bb261854163b0566f6cea3d3ebc"}'></script>
    <!-- End Cloudflare Web Analytics -->
  </head>
  <body>
    <slot />
    <ConsoleSignature />
  </body>
</html>
