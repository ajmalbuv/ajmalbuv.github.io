<script>
  import { PUBLIC_GIT_HASH, PUBLIC_BUILD_TIME } from "astro:env/client";

  const gitHash = PUBLIC_GIT_HASH || "dev";
  const buildTime = PUBLIC_BUILD_TIME || new Date().toISOString();
  const formattedBuildTime = new Date(buildTime).toLocaleString();
  const browserLocale = Intl.DateTimeFormat().resolvedOptions().locale;

  const mauve = "color: #cba6f7";
  const blue = "color: #89b4fa";
  const peach = "color: #fab387";
  const overlay = "color: #6c7086";
  const green = "color: #a6e3a1";
  const reset = "color: inherit";
  const bold = "font-weight: bold";

  const fontFamily =
    'font-family: "FiraCode Nerd Font", "Fira Code", monospace';

  console.log(
    `%c%c\n` +
      `%c 🚀 AJMAL BASHEER %c| %cSoftware Engineer & Systems Builder\n` +
      `%c ───────────────────────────────────────────────────────────\n` +
      `%c 📍 Based:     %cKerala, India\n` +
      `%c 🔗 GitHub:    %chttps://github.com/ajmalbuv\n` +
      `%c 🔗 LinkedIn:  %chttps://linkedin.com/in/ajmalbuv\n` +
      `%c 📧 Email:     %cajmalbuv@gmail.com\n` +
      `%c ───────────────────────────────────────────────────────────\n` +
      `%c 👀 DIGGING AROUND, ARE WE?\n` +
      `%c 📂 Repo:      %chttps://github.com/ajmalbuv/ajmalbuv.github.io\n` +
      `%c ───────────────────────────────────────────────────────────\n` +
      `%c [ Build: %c${gitHash} %c| Locale: %c${browserLocale} %c| Compiled: %c${formattedBuildTime} %c]`,
    `${mauve}; ${bold}; ${fontFamily}`,
    reset,
    `${mauve}; font-size: 14px; ${bold}; ${fontFamily}`,
    "color: #585b70",
    "color: #9399b2; font-style: italic",
    "color: #313244",
    peach,
    reset,
    peach,
    blue,
    peach,
    blue,
    peach,
    blue,
    "color: #313244",
    `${mauve}; ${bold}`,
    peach,
    blue,
    "color: #313244",
    overlay,
    mauve,
    overlay,
    green,
    overlay,
    peach,
    overlay,
  );
</script>
